package me.gopro336.zenith.managment;

import me.gopro336.zenith.api.util.IGlobals;

public class ScreenManager implements IGlobals {
    /*public static Screen screen = Screen.Click;

    public static void setScreen(GuiScreen guiScreen) {
        mc.displayGuiScreen(guiScreen);

        if (guiScreen instanceof ClickGUI)
            screen = Screen.Click;
        else if (guiScreen instanceof HUDEditor)
            screen = Screen.Hud;

        //mc.entityRenderer.loadShader(new ResourceLocation("shaders/post/blur.json"));
    }

    public enum Screen {
        Click,
        Hud
    }*/
}
